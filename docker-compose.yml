services:
  ocr-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: social-wifi-ocr-api
    ports:
      - "8001:8001"
    env_file:
      - .env
    environment:
      PORT: 8001
    restart: unless-stopped
